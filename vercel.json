{
  "version": 2,
  "builds": [
    {
      "src": "apps/web-ele/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "apps/web-ele/dist"
      }
    }
  ],
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
