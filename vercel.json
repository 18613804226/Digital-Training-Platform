{
  "version": 2,
  "builds": [
    {
      "use": "@vercel/static-build",
      "config": {
        "distDir": "apps/web-ele/dist"
      }
    }
  ],
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
